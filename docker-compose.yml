services:
  web:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
    volumes:
      - navtools-data:/app/data
    restart: unless-stopped

volumes:
  navtools-data:
